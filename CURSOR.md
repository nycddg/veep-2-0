# Open this in Cursor

Path: `/opt/data/projects/veep-work-fable`

Fable 5: send model params. Bare `claude-fable-5` returns empty.
Dave ChatGPT pick for Sol was `context=1m`, `reasoning=medium`, `fast=false`. For Fable, pass the catalog `thinking` / `context` / `effort` values.

First message to Fable:

> Read LOCKS.md and PLAN.md. Do Pass 0 only. Write AUDIT.md. Do not edit product files until I say go.

Do not open `/opt/data/veep-2-0`.
